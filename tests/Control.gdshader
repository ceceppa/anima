shader_type canvas_item;

uniform float speed = 10.0;
uniform vec2 v2 = vec2(1.3, 0.3);
uniform vec2 v2_1 = vec2(1.3);
uniform vec3 v3 = vec3(1.9);
uniform vec3 v3_1 = vec3(1.3, 3., 0.5);
uniform vec4 c: hint_color = vec4(1.);
uniform vec4 c_1 = vec4(1.3, 3., 0.5, 0.0);