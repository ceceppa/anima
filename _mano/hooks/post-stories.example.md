# post-stories hook

## Mode
check

## Inputs
- the exact `PHASE_DIR/stories/` and `STORIES` index from the state projection
- the exact `BRIEF` path from the state projection
- `_mano_output/tech-spec.md`, `_mano_output/project-rules.md`, `_mano_output/design-brief.md`, `_mano_output/ux-flow.md`, if they exist

## Checklist

<!-- Your checks, one `- ` line each, applied by Mano after every `mano stories`.
     Uncomment what you want and edit freely — these are examples, not defaults.
     An active hook with nothing uncommented has nothing to apply.

- At least one AC verifies each distinct outcome and quality word from the phase goal.
- Each acknowledged risk in the brief is exercised by an AC or explicitly flagged in Notes.
- Acceptance criteria are observable behaviour — no internal data structures, function names, formulas, or implementation style.
- Each story is verifiable through a real interface when it lands — no orphan stories with no externally verifiable exit.
- Each interactive story names its surface, trigger, and entry path.
- Implementation Reference paths and ownership are unambiguous — no "A or B", no "if not yet defined".
- No story carries more than one focused session of work.
-->

Findings are diagnostic, not prescriptive: name the problem and the affected story file; never draft replacement AC text. Done stories still require lettered corrective work.
