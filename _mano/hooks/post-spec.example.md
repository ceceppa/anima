# post-spec hook

## Mode
check

## Inputs
- `_mano_output/tech-spec.md`
- the exact `BRIEF` path from the state projection
- project manifest and lockfile, if they exist

## Checklist

<!-- Your checks, one `- ` line each, applied by Mano after every `mano spec`.
     Uncomment what you want and edit freely — these are examples, not defaults.
     An active hook with nothing uncommented has nothing to apply.

- API contract shape, data model, and persistence assumptions are internally consistent.
- No contradiction or omission relative to the phase brief.
- Edge cases the phase depends on are addressed, not implied.
- Install commands match the actual package manager evidence.
-->
