# post-ux hook

## Mode
check

## Inputs
- `_mano_output/ux-flow.md`
- the exact `BRIEF` path from the state projection

## Checklist

<!-- Your checks, one `- ` line each, applied by Mano after every `mano ux`.
     Uncomment what you want and edit freely — these are examples, not defaults.
     An active hook with nothing uncommented has nothing to apply.

- Every screen is reachable from a defined entry point — no orphans.
- Every screen has a clear way back, forward, or out.
- No screen handles more than two primary product actions or decisions.
- Choice-dependent paths are fully described; branches do not trail off.
- The flow covers the phase goal's user-visible outcomes.
- Loading, empty, and error states are named where they materially affect the user.
-->
